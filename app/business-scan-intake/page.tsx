import BusinessScanIntake from '@/components/BusinessScanIntake';

export const metadata = { title: 'Business Scan Intake: Energieke Lieke' };

export default function Page() {
  return <BusinessScanIntake />;
}
